'use client';

/**
 * Compatibility wrapper.
 *
 * Historically this component rendered a pre-launch landing view.
 * The platform is now feature-complete; this file remains only to avoid
 * breaking any legacy imports.
 */

export { default } from './MaintenancePage';
