# Lint Warning Backlog

This living backlog captures the most frequent severity-1 warnings and who is responsible for cleaning them up. Each entry points to the mitigation steps, target timeframe, and blockers so the `Top warning rules cleanup` initiative stays on course without duplicating the broader playbook text or ops run reports. Entries should cite the latest `lint-summary.txt` counts and reference the corresponding `lint-report.json` blob when extra context is needed.

| Rule | Severity 1 count (Nov 18) | Owner | Mitigation focus | Target date | Notes |
|------|--------------------------|-------|------------------|-------------|-------|
| `no-unused-vars` | 1518 | Engineering (Shared helpers squad) | Remove unused exports/constants, convert redundant enums into usage (or `_` prefix placeholders). Focus on `src/lib` helpers and `src/types`. Run `LINT_TARGET=src/types/scripts run-lint.sh` to revalidate per package. | Nov Week 4 | Blockers include `snity-check.md` research backlog and intentionally exported constants for copy decks; add targeted `_` prefixes and document in `docs/ops/lint_playbook.md` when exceptions remain. Shared types enums have been refactored into `const` objects so a `src/types` run now reports zero warnings (`lint-report.json` and the ops run report capture this), giving us confidence to expand the cleanup to the next helper bundle. |
| `react/no-unescaped-entities` | 56 | Content + Frontend (Content QA + Localization) | Escape punctuation in help/training copy or use localized strings already sanitized. QA translation team should verify identical renders don’t reintroduce `'`/`&`. | Dec Week 1 | Pair fix with `docs/TRAINING_VIDEO_STATUS.md` updates to prevent re-copying unescaped text into video scripts. |
| `react-hooks/exhaustive-deps` | 36 | Engineering (Hook ownership guild) | Audit `src/app` intervention hooks and services; hoist stable callbacks, add `useRef` for mutable helpers, and add explicit dependency arrays. | Dec Week 1 | Add unit tests for affected hooks when altering dependencies to guard runtime behavior. |
| `@next/next/no-img-element` | 23 | Platform (Landing + Help platform squad) | Replace residual `<img>` tags in `src/app/help`, `src/app/landing`, and localized help articles with `<Image>` or vetted helpers covering decorative assets. | Dec Week 2 | Document exception for animation assets requiring raw `<img>` with accessible fallbacks in this backlog. |
| `@typescript-eslint/no-require-imports` | 22 | Backend (Schema + Services team) | Refactor legacy service helpers (including `test-db-connection.js`) to use `import`. Update `tsconfig` for consistent module resolution and adjust Jest mocks if necessary. | Jan Week 1 | This overlaps with schema migration (see `prisma/schema.prisma` changes). Schedule pair review to avoid conflicting runtime behavior. |

## Process notes
- After each owner batch lands, run `bash tools/run-all.sh` (or `LINT_TARGET=... bash scripts/run-lint.sh`) to regenerate `lint-report.json`, `lint-summary.txt`, and the ops run report so the new row reflects reduced counts.
- Assign any residual warnings that do not match the table to `docs/ops/cleanup_plan.md` run history entries so we maintain a single source for exceptions.
- Update this backlog whenever a rule shifts priority so the ops run report and roadmap (e.g., `docs/MIGRATION_PROGRESS.md`) can annotate the specific `Target date` and `Notes` adjustments.
